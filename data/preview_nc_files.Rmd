```{r}
library(nctools)
library(Ipaper)
```

```{r}
fs = dir2("data", "*.2021.nc$", recursive = FALSE)
fs
```
```{r}
ncread(fs[2], "level")
ncread(fs[3], "level")
ncread(fs[4], "level")
# ncinfo(fs[3])
```

```{r}
map(fs, nc_date)
```
